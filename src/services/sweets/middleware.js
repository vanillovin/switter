import type from './typeActions';
// import axios from 'axios';
// import { API_URL } from '../config'

export const applyMiddleware = (dispatch) => (action, getState) => {
  // switch (action.type) {
  //   case type.FETCH_SWEETS_STARTED:
  //     return
  // }
};
